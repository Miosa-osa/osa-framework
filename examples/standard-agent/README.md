# Standard Agent

This is the clean starter. It is intentionally one file.

```text
agent/
  instructions.md
```

Use this when you want people to make their own agent instead of starting from
a heavy demo.

Try it:

```bash
npx @miosa/osa build .
```

Then edit `agent/instructions.md` with your actual agent behavior.
