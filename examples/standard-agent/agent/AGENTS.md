# Standard Weather Agent

This example should stay readable as a directory. Each capability belongs in the
slot that names it: instructions in Markdown, tools in TypeScript, skills in
Markdown, and recurring work in schedules.
